example = [[1,'a',['cat'],2],[[[3]],'dog'],4,5]
flatten = []
def flatter(e,flatten):
    for item in e:
        if type(item) != list:
            flatten.append(item)
        else:
            flatter(item,flatten)

flatter(example, flatten)
print(flatten)
